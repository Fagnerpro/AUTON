Resumo dos Erros
removeChild is not a child of this node

Erro: NotFoundError: Failed to execute 'removeChild' on 'Node': The node to be removed is not a child of this node.

Origem: O React tentou remover um nó DOM que já não está mais presente no pai informado.

insertBefore is not a child of this node

Erro: NotFoundError: Failed to execute 'insertBefore' on 'Node': The node before which the new node is to be inserted is not a child of this node.

Origem: O React tentou inserir um nó antes de outro que não pertence mais ao DOM.

📌 Causas Comuns
Renderizações assíncronas concorrentes: especialmente com React Query, Suspense, useEffect, ou setTimeout.

Manipulação imperativa direta no DOM fora do controle do React (ex: element.remove() ou appendChild()).

Condicionais de renderização incorretas ou mudanças abruptas no key de componentes em listas.

Componente desmontado prematuramente, mas com efeitos ainda ativos (ex: setTimeout, fetch, useEffect).

Erro em árvores aninhadas no Switch do wouter, ou React Router, removendo ou reinserindo componentes do DOM antes do tempo.

✅ Recomendações Técnicas
Garanta consistência de chaves (key) em listas:

tsx
Copiar
Editar
{data.map((item) => (
  <Component key={item.id ?? item.uuid} />
))}
Evite renderização condicional errática sem fallback:

tsx
Copiar
Editar
{isMounted && <Component />} // ok
{data && <Component />} // cuidado: se data for falsy por um ciclo, pode quebrar
Use useEffect com cancelamento de efeito:

tsx
Copiar
Editar
useEffect(() => {
  let mounted = true
  fetchData().then((res) => {
    if (mounted) setState(res)
  })
  return () => {
    mounted = false
  }
}, [])
Evite manipular DOM diretamente (sem ref) dentro de componentes React.

Valide se há renderização paralela e sobreposição de efeitos

Quando estiver usando React 18 com StrictMode, o comportamento de montagem e desmontagem é duplicado em modo dev.

Se usar animações, limpe efeitos:

tsx
Copiar
Editar
const timeoutId = setTimeout(() => ..., 500)
return () => clearTimeout(timeoutId)
🧰 Sugestão de Auditoria
Se quiser investigar de forma mais estruturada:

Ative React DevTools Profiler para rastrear renderizações redundantes.

Adicione console.log nos ciclos de vida (useEffect, componentDidMount) para entender a sequência de montagem/desmontagem.

Verifique se há setState() sendo chamado após desmontagem.

📄 Exemplo com ErrorBoundary
Você já está usando um ErrorBoundary, o que é excelente. Certifique-se de que ele está envolvendo apenas componentes que não manipulam diretamente o DOM fora do React.